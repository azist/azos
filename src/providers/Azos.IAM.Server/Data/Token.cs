using System;
using System.Collections.Generic;
using System.Text;

namespace Azos.IAM.Server.Data
{
  /// <summary>
  /// Provides backing store for TokenRing implementation
  /// </summary>
  public sealed class Token : Entity
  {
  //todo:  RingToken ToRingToken();  void FromRingToken(RingToken token);
  }
}
